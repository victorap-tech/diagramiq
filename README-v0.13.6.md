# DiagramIQ v0.13.6 — Vision con análisis real

- Vista previa antes de enviar la foto.
- Botón explícito **Analizar imagen**.
- Corrige orientación EXIF y reduce fotos grandes.
- Convierte la captura a JPEG compatible antes de enviarla a Claude.
- Usa Anthropic automáticamente cuando existe `ANTHROPIC_API_KEY`.
- Devuelve TAG, referencia, marca, modelo, tipo y confianza.
- Permite buscar el resultado en planos o seguir el circuito.
- Agrega `GET /vision/status` para diagnóstico.
