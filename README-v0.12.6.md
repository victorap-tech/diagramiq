# DiagramIQ v0.12.6

- El asistente prioriza la referencia consultada como componente principal.
- Potenciales y etiquetas ambiguas como PE, N, L1, M y colores de conductor nunca generan la tarjeta principal.
- El visor solicita un render de alta resolución del PDF original (escala 3) y conserva el resaltado exacto.
- El backend cachea temporalmente las páginas de alta resolución para acelerar aperturas posteriores.
